/** 
  * 用户列表 
  */ 
layui.use(['form' ,'table'], function() { 
	var $ = layui.$; 
    var _table = layui.table,_form = layui.form;
//    _table.render('user', {
////  	  height: 315 //设置高度
////  	  ,limit: 10 //注意：请务必确保 limit 参数（默认：10）是与你服务端限定的数据条数一致
//  	  //支持所有基础参数
//  	});
})  